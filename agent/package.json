{
  "name": "@ganado/bridge-agent",
  "version": "0.2.0-preview.0",
  "private": true,
  "type": "module",
  "description": "Local MCP file/process tools with an outbound managed-relay preview client.",
  "license": "MIT",
  "engines": {
    "node": "^22.22.0 || ^24.0.0"
  },
  "bin": {
    "ganado-bridge": "bin/bridge.mjs"
  },
  "files": [
    "bin",
    "src",
    "README.md",
    "EVALUATION.md",
    "npm-shrinkwrap.json"
  ],
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check bin/bridge.mjs && node --check src/main.mjs && node --check src/server.mjs && npm test",
    "doctor": "node bin/bridge.mjs doctor",
    "selftest": "node bin/bridge.mjs selftest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "4.6.5"
  },
  "mcpName": "io.github.cynarax/bridge-by-ganado"
}
